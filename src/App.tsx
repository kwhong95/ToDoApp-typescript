import React from 'react';
import { ContextProvider } from './Context';

const App: React.FC = () => {
  return (
    <ContextProvider>
      {/* Component */}
    </ContextProvider>
  )
}

export default App;

